<%= form_for([@product, @selling_value]) do |f| %>
  <% if @selling_value.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@selling_value.errors.count, "error") %> prohibited this selling_value from being saved:</h2>

      <ul>
      <% @selling_value.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_field :value %>
  </div>
  <div class="field">
    <%= f.hidden_field :product_id, value: @product.id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
